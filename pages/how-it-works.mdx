# How it works

Here we will explain how our SDK interacts with fuel and how it was built.

## Bsafe Vault

Our Vault is developed based on the Fuel Predicate, when it is built with its ABI and its Buffer is
A Fuel address is generated where you can store all your assets.

When executing a transaction, validation is performed if all addresses signed this transaction, if true the transaction is executed.
